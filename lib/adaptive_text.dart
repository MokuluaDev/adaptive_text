/// A Flutter widget that intelligently adapts text display based on available space
/// with a three-tier fallback strategy: line wrapping, font scaling, and truncation.
///
/// This library provides the [AdaptiveText] widget which automatically adjusts
/// text presentation to fit within the available space while maintaining readability
/// and accessibility.
library adaptive_text;

export 'src/adaptive_text.dart';
